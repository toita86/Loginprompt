<h2><%= auser.name%></h2>
<a href="/users/<%=user.id%>/edit">edit</a>

<%- include('../partials/deleteForm', {url: `/users/${user.id}`})%>
    
<% if(colorOfUser.length > 0){ %>
    <h2>Color</h2>
    
<% } %>